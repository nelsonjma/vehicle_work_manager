var rowContent = '<tr>' +
        '<td><%= @work.description  %></td>' +
        '<td><%= @work.notes %></td>' +
        '<td><%= @work.finished ? 'sim' : 'não' %></td>' +
        '<td><%= link_to 'Tarefas', work_tasks_path(params: { work_id: @work.id }), class: 'btn btn-info' %></td>' +
        '<td><%= link_to 'Editar', edit_work_path(@work.id.to_s), remote: true, class: 'update_post btn btn-warning' %></td>' +
        '<td><%= link_to 'Eliminar', work_path(@work.id.to_s), method: :delete, data: { confirm: 'Tem a certeza?' }, remote: true, class: 'delete_post btn btn-danger' %></td>' +
        '</tr>';

insertRowAfter('work_tbody', rowContent);

// close create/edit modal
$('#addEditModal').modal('hide');