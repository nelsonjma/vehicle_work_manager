$('#container').html("");

var row_position = $('#table_row_position').val();

var selected_row = $('#tasks_tbody').find('> tr:eq(' + row_position + ')');

selected_row.find('> td:eq(0)').html("<%= @task.name %>");
selected_row.find('> td:eq(1)').html("<%= @task.description %>");