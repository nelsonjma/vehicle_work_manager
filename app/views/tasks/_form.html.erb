<%= form_for(@task, remote: true) do |f| %>
  <div class="field">
    <%= f.label 'nome:' %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label 'descrição:' %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
